# downloads/

Local-only collection of binaries (firmware packages, tweak bundles, PDFs).
Everything here except this file and `CHECKSUMS.sha256` is git-ignored.

Every file placed here MUST be recorded in `research/INVENTORY.md` and hashed into `CHECKSUMS.sha256`
(`sha256sum <file> >> CHECKSUMS.sha256`).
